# 评测运行时产物（不入库）
eval_outputs/
cache/
.local_state.json

# 本地评测配置（含 API key 等凭证，禁止入库）
evalspec*.yaml
!assets/evalspec.template.yaml

# 用户自定义 metric（本地资产，不入库）
custom_metrics/*.py

# 临时
*.pyc
__pycache__/
